{% set name = "atmospheric_correction" %}
{% set version = "0.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  git_url: https://github.com/JorisTimmermans/atmospheric_correction.git

build:
  number: 0
  noarch: python
  script: "python  setup.py develop"

requirements:
  build:
    - git
    - python=3.6.7=h357f687_1006
    - gdal=2.4.2=py36h5f563d9_8
  host:
    - python=3.6.7=h357f687_1006
    - gdal=2.4.2=py36h5f563d9_8
  run:
    - python=3.6.7=h357f687_1006
    - gdal=2.4.2=py36h5f563d9_8